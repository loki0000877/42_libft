#include <ft_libft.h>

size_t  ft_strlcpy(char *dest, const char *src, size_t dsize)
{
  size_t  i;
  size_t  len;

  len = ft_strlen(src);
  if (dsize == 0)
    return (len);
  i = 0;
  while (i < dsize - 1 && src[i])
  {
    dest[i] = src[i];
    i++;
  }
  dest[i] = '\0';
  return (len);
}
