#include <libft.h>

int  ft_atoi(const char *nptr)
{
  unsigned int  i;
  int  sign;
  

  i = 0;
  while ft_isspace(nptr[i])
    i++;
  if (nptr[i] == '-')
    sign = -1;
  i++;
  while (ft_isdigit(nptr[i]) && nptr[i])
    
